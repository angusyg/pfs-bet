/**
 * @fileoverview Players module controller
 */
(function() {
  'use strict';

  angular
    .module('frontend.admin.players')
    .controller('PlayersController', PlayersController);

  PlayersController.$inject = [];

  function PlayersController() {
    const vm = this;

    /** variables */

    /** functions */
  }
}());
